# 2026-08-11：OBJ-002 Harness 流程复盘

## 工作单元

- Objective：`OBJ-002` 复盘 Askme 全量交付 Harness 流程并形成受控演进提案
- Plan：`PLAN-007`
- 分支：`docs/harness-evolution-analysis`
- 预期 Commit：`docs: analyze harness delivery closure gaps`

## 本次交付

- 从 `OBJ-001` 六份 Plan、单一 Spec、四份 Design、26 份 Review、五份 Operation、E2E 脚本与七次交付相关 Commit 恢复当前流程事实。
- 将用户指出的 Spec、Journal、Session Review、Scenario 四类缺口分别归因为 Harness 模型/顺序缺口、Agent 执行遗漏和关闭门禁缺口。
- 形成 `EVO-001` Proposal，定义 Plan-owned capability Spec、稳定 Scenario、Commit 前 Session Review/Journal 和确定性 trace validator；未修改或应用根治理。
- 创建 `REVIEW-027` Plan Review 与 `REVIEW-028` Change Review，并同步 Progress 与 docs Index。

## 关键决定

- Spec 仍只拥有外部行为、边界与 AC；低层技术方案继续由 Design 拥有，不以“技术 Spec”为名复制实现细节。
- Standard Commit 必须有轻量 Session Review 结果和 Journal；Fast 默认不制造详细文档。
- Git 不能直接调用 Codex Skill，因此采用“Agent 自动路由 + 确定性 pre-commit close check”双层方案，而不是把 Git hook 当作 Skill 执行器。
- 历史完成 Plan 只 audit warning，新活动 Plan 才 strict，不伪造或回填历史。

## 验证

- `git diff --check`：PASS。
- docs Index builder：计划、Review、Evolution 索引重建成功；关闭前将包含本 Journal 再次幂等重建。
- `python3 .agents/skills/autogo-harness-validate/scripts/validate_harness.py --root . --strict`：`0 errors, 0 warnings`。
- `REVIEW-028`：`PASS`。
- 未修改产品实现，未运行单元、Docker 或浏览器回归，也未将其报告为当前验证。

## Session Review

结果：`PROPOSAL` → [EVO-001](../harness/evolution/EVO-001.md)

有效做法是 Plan、Review、Operation 与 Git Evidence 能够完整恢复实现事实；主要摩擦是非主干制品依赖自然语言触发，Capability 存在但可被静默跳过。改进优先使用有限决定、唯一 owner 和只读 validator，没有新增平行状态机。

## 未完成与恢复

- `EVO-001` 仍为 `proposed`，根 `AGENTS.md`、Skills 和 validator 均未修改。
- 精确恢复条件：用户明确批准 `EVO-001` Human Gate 后，新建独立 Harness Evolution Objective 实施；未批准时保持当前运行合同不变。
- 回滚当前文档交付只需还原本次原子 Commit，不影响产品代码、数据库或 Docker 数据。
