# 2026-08-13：PLAN-014 SPEC-002 真实环境 Human Gate

## 当前状态

- Objective `OBJ-009` 与 [PLAN-014](../plans/PLAN-014.md) 保持 `正在处理`；Phase 1 至 Phase 6 已完成并通过 [REVIEW-065](../reviews/REVIEW-065.md)，Phase 7 的 7.1、7.2、7.3、7.6、7.7、7.8、7.10 已完成。
- 当前分支 `feat/repository-code-agent-v1`，已提交基线为 `28cd20c`；本次 Standard run 的实现与文档仍未提交。
- 当前 Compose 仍运行旧镜像，PostgreSQL 已应用 `0001` 至 `0011`，`db/web/worker` 正常；本次隔离 host-native `agent-runner` 已完成健康验证并停止，当前主数据库 runner 尚未部署。

## Waiting 原因

`0013_remove_github_material_kind.sql` 会拒绝仍存在的旧 GitHub Source Material。当前数据库有且仅有 1 条此类 material（id `8e9d78fa-568d-4bae-8adb-5b2bb34df43b`，`indexed/public_preview`），删除会级联影响：

- 28 个 Chunk；
- 36 条 Knowledge Evidence；
- 12 个仅由该 GitHub material 支撑的 Knowledge Item；
- 1 个历史回答的 2 条旧 material Citation；
- 1 个 ingestion job 与 12 条 knowledge source 关联。

其他当前数据基线为 2 条 file material、2 个 conversation、16 条 message、6 条 publication。按 `SPEC-002 §1`，真实破坏性删除仍需用户单独授权，不能用“自行决策”替代该 Human Gate。

此外，宿主与当前 Web 容器均未配置 `ASKME_AI_API_KEY`，宿主也未配置 `ASKME_GITHUB_TEST_TOKEN`。因此固定 public Repository 的真实上游模型质量验收和固定 private Repository 的一次性 Token 验收尚不能执行；本地 OpenAI-compatible fixture Evidence 不能替代这两项。

## 已完成准备

- 只读核对了 Compose 服务、volume、migration、数据计数、外键级联和旧来源影响范围；未修改数据库。
- 已创建并通过 `pg_restore -l` 校验的全库 custom-format 备份：`/tmp/askme-pre-spec002.Z5djoZ/askme.dump`。
- 备份权限 `0600`，大小 `190078` bytes，SHA-256 `bbca7ca9f1852026c27f3bd144b9460712b383553cbbcebc1c60b23f31087e1b`。
- 已确认 `data/code-agent-image` 的真实 BoxLite image 在前序 runner smoke 中可用，但真实部署仍需 `scripts/agent-runner.sh` 启动并产生数据库心跳。
- 已在独立 `askme_spec002_e2e_20260813` 数据库与 `3200` 端口同时运行当前 Web、worker、host runner、Artifact Store、BoxLite 与本地 AI fixture；全 readiness、跨重启持久性、24/25 package 入口、59 路径 / 65 method、18 页面 × 2 viewport 和 Candidate/Public/Admin 主旅程通过。详细 Evidence 见 [SCN-002](../scenarios/SCN-002.md)。
- 隔离数据库与 fixture 已精确清理；临时目录移动到 `/Users/tanzhangyu/.Trash/askme-spec002-e2e.VgENh6-20260813`，可从废纸篓恢复。现有 `askme` 数据库和 Compose volume 未修改。

## 精确恢复入口

用户明确批准删除后：

1. 在单一 PostgreSQL transaction 中只删除上述 12 个纯旧来源 Knowledge Item 与 1 条旧 GitHub material；依赖记录按现有外键级联，不处理其他 material、conversation、message 或 publication。
2. 对账删除前后数据计数并再次校验备份文件；随后使用 `scripts/docker-up.sh -d` 保留现有 volume 部署 migration、Web 与 worker。
3. 启动 `scripts/agent-runner.sh`，验证 `/api/health/ready` 的主 readiness 与 Code Agent capability、Admin Settings 健康卡和数据库 heartbeat。
4. 用户只需在 `~/.env` 自行配置 `ASKME_AI_API_KEY` 与 `ASKME_GITHUB_TEST_TOKEN`，不要在对话中发送值；检测到键存在后继续固定 public/private Repository 外部质量与泄露验收。全页面、全 API 与隔离全组件浏览器回归已完成，不重复执行无关部分。
5. 若删除或部署产生不可接受结果，停止应用写入并从上述备份恢复整个本地数据库；恢复前再次校验 SHA-256。
