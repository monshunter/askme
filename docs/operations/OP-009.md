# OP-009：PLAN-025 Entity-grounded RAG V3 重建、部署与真实验收

状态：`verified`

关联 Plan：[PLAN-025](../plans/PLAN-025.md)

关联 Spec：[SPEC-002](../specs/SPEC-002.md)

关联 Design：[DESIGN-005](../architecture/DESIGN-005.md)

适用环境：本机 Docker Compose `askme-local`

验收日期：2026-08-15

## 数据备份、重建与索引切换

- 维护前使用 PostgreSQL custom dump 生成 `/tmp/askme-pre-entity-grounded-rag-v3.dump`，大小 `3,382,233 bytes`。为对账曾把该备份恢复到临时数据库 `askme_rag_pre_audit`；查询结束后已删除临时数据库，未触碰运行库。
- 维护前备份计数为 `users=4 / materials=4 / repositories=3 / publications=9 / conversations=50 / messages=222 / knowledge_items=39 / active_sources=92 / active_children=428`。
- 使用项目维护入口执行 Knowledge organization 与派生 RAG 全量重建并原子激活。最终为 `knowledge_items=32 / entity_values=86 / active_sources=92 / active_children=417`，不存在空实体 Knowledge Item。
- 唯一 active index 为 `388863ea-4cf2-40e6-8419-3e60d3571bf7`，`context_prefix_version=source-entity-context-v2`；旧 `source-context-v1` index 为 `superseded`，不再参与查询。
- 最终业务计数为 `users=4 / materials=4 / repositories=3 / publications=9`，与备份一致。最终 `conversations=50 / messages=252`；验收创建 3 个隔离 Conversation 和 30 条消息，并在既有浏览器会话增加 4 条消息；现有 public retention 同期按 24 小时 TTL 删除了 3 个到期会话及其 4 条消息，因此 Conversation/Message 的最终净变化分别为 `0 / +30`，不是知识重建的数据损失。维护脚本未删除账号、Material、Repository、Publication 或会话。

## 部署与运行状态

- 所有部署均使用 `scripts/docker-up.sh -d`，保留 `askme_local_pgdata`、uploads 与 Repository Artifact；没有执行 reset 或 volume 重建。
- 最终 production Build 编译成功并生成 `31 / 31` 页面；migrate exit 0、Web healthy、worker running。
- `/api/health/ready` 顶层为 `ready`，database、migration、worker 为 `ready`，AI 为 `configured`。近 10 分钟 web/worker/migrate 日志没有匹配到 error、exception、fatal、panic、unhandled 或 warn。
- Code Agent 的 runner stale、artifact degraded、BoxLite unavailable 和 provenance unverified 是本次开始前已存在的独立状态；普通 RAG、Citation 与实体门禁不依赖它，本次没有把该状态描述为恢复。

## 评测 Evidence

- 离线核心评测真实调用生产 Query Analyzer、Entity Resolver、visibility/scope filter、weighted RRF 与 Evidence Pack：120 个 synthetic retrieval case 的 `initialRecall@30=1`、`rerankRecall@8=1`、permission refusal rate `=1`，visibility/forbidden selection leak `=0`；12 个 entity-grounding 与 10 个 entity-query case 均无失败。
- 离线评测的 `provisionalCoverageAgreement=0.541666...` 只作为启发式诊断；脚本明确声明不调用数据库/Provider，也不据此声称最终 Answerability、答案 outcome 或 Citation precision。
- Compose 网络中的真实 PostgreSQL/Provider 评测为 `21 / 21 PASS`：19 条覆盖已知/未知实体、无类型 Alias、大小写与分隔符、短 Alias 嵌套、多实体 partial、唯一/缺失/歧义指代；2 条分别注入 Embedding 与 Rerank 故障，仍从其余真实生产链路返回有 Citation 的 Askme 回答并记录 `embedding_fallback` / `rerank_fallback`。
- 真实 HTTP 评测为 `11 / 11 PASS`：Candidate `6` 条、Public `5` 条；两端各有 3 条 grounded answer，Candidate 3 条、Public 2 条 deterministic failed-close。未知 `MoonBase` 在 round 0 返回 `INSUFFICIENT_EVIDENCE` 和 0 Citation，多实体问题只回答 Askme 并列出 MoonBase 缺口；未知源码问题没有创建 Deep Run；Public Citation 不暴露内部 chunk ID；全部 Trace 使用 `entity-grounded-rag-v3`。
- 首轮 API 验收发现 contextual ambiguity 被 LLM Router 改写成 `REFUSED`。实现已 Reconcile 为 Host entity gate 在 `stopBeforeRetrieval` 时跳过 Router并直接形成证据不足结果；定向测试和同一 HTTP 场景复跑后通过。

## 真实浏览器 Evidence

- Public Agent `/a/qyFssf9_WMGnsiOC7K0GP6I4cUyVSr44`：`Askme 怎么样？` 返回中文有依据回答和 1 个 `SPEC.md` 公开 Citation；`MoonBase 怎么样？` 显示“需要更多证据”，不显示来源。
- 展开 `SPEC.md` Citation 后可读取已授权正文；刷新页面后 Askme 回答、MoonBase 证据不足状态和 Citation 均恢复。Public 页面 Console 日志为 0。
- Candidate `/workspace/agent`：多实体问题展示 Askme 回答与独立“证据缺口”；其后的“它”因上一轮同时含 resolved Askme 与 missing MoonBase 而显示歧义提示；未知实体与未知源码问题均显示“需要更多证据 / 未暴露来源”。
- Candidate Citation 侧栏可展开 Retrieval Trace，显示 `Coverage=partial`、route/evidence/budget 与 `Policy=entity-grounded-rag-v3`。Candidate 页面 Console 日志为 0。

## 最终自动化门禁与边界

- Vitest `104 files / 403 tests`、ESLint、Next typegen + TypeScript、production Build、Surface Matrix `22 pages / 68 API routes / 76 methods / 29 verification entrypoints` 与 `git diff --check` 全部通过。
- 本记录证明当前本机 Compose、当前真实 Candidate 数据、真实 Provider、Candidate Preview 和 Public Chat 可用；不外推为生产容量、跨租户规模或 Code Agent 恢复证明。
