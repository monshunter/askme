# OP-001：Askme 本地 Docker 基础环境

状态：`active`

关联 Plan：[PLAN-001](../plans/PLAN-001.md)
适用环境：本机 `askme-local` Docker Compose project

## 前置条件与边界

- Docker Engine 29.4.0、Docker Compose 5.1.1。
- 宿主端口仅绑定 loopback：Web `127.0.0.1:3000`，PostgreSQL `127.0.0.1:55432`。
- `scripts/docker-up.sh` 优先使用当前进程环境；存在 `~/.env` 时作为 Compose env file 补充。只有 Compose 声明的变量会进入容器。
- Compose 将 database、runtime/AI 与 bootstrap 凭证分为最小环境集合；账号明文密码只进入一次性 migrate/bootstrap 容器，DeepSeek key 只进入 Web/worker。
- 本操作只管理 `askme-local` 的 db、migrate、web、worker、`askme_local_pgdata` 和 `askme_local_uploads`；不得操作其他项目容器。

## 启动与验证

```bash
scripts/docker-up.sh -d
docker compose ps
curl -fsS http://127.0.0.1:3000/api/health/ready
npm run smoke:auth
```

本地 bootstrap 账号来自 `ASKME_CANDIDATE_*` 和 `ASKME_ADMIN_*`；示例值见 `.env.example`。bootstrap 只在账号不存在时创建，不自动覆盖已有密码。

## 当前 Evidence（2026-08-11）

- `0001_initial.sql` 在 PostgreSQL 18 成功应用，`schema_migrations=1`，public schema 共 20 张业务表。
- bootstrap 第一次报告 Candidate/Admin 均创建，第二次报告均未创建；数据库聚合为 `candidate:active:1`、`admin:active:1`。
- `npm audit --audit-level=moderate`：0 vulnerabilities。
- `npm test`：3 files / 8 tests PASS；`npm run lint`、`npm run typecheck`、`npm run build` PASS。
- Docker image build PASS；db/web 均 healthy，ready 返回 database/migration/worker ready、AI configured。
- 真实 DeepSeek availability：`deepseek-v4-flash` 完成最小请求，安全输出 latency 652 ms，未记录 key 或响应正文。
- HTTP auth smoke：live/ready 200；Candidate/Admin 各自登录并解析正确角色；Candidate 访问 Admin 重定向；logout 303；旧 session 随后 401。
- 非破坏性 `docker compose restart db web worker` 前后 `users=2`、`migrations=1`；新 worker heartbeat 生效，Web 恢复 healthy，auth smoke 再次 PASS。
- Change Review 修复后，Compose config 证明 db/web 仅绑定 `127.0.0.1`，migrate 不含 DeepSeek key，Web/worker 不含 bootstrap 密码，db/web/worker 均使用 `unless-stopped`。
- 受控停止 db 12 秒时，worker 从 `running` 进入自动 `restarting`，`RestartCount` 从 0 增至 6；同一 db volume 恢复后 worker 自动回到 running，ready 全部通过，且 `users=2`、`migrations=1` 未变化。

以上只证明 `PLAN-001` 的全栈基础，不证明资料、知识库、隐私、Agent、发布、公共 Chat、Admin 产品页或最终 UI 已完成。

## 已处置失败

1. PostgreSQL 18 拒绝把 volume 挂载到 `/var/lib/postgresql/data`。日志要求 18+ 挂载 `/var/lib/postgresql`；只读检查确认本轮新建 volume 为空后，删除该空 volume 并按新路径重建，随后 migration 成功。
2. standalone Web 默认绑定容器 hostname，导致 Docker healthcheck unhealthy 且登录 redirect 指向内部 hostname。Compose 现仅为 Web 设置 `HOSTNAME=0.0.0.0`，HTTP redirect 从外部 Host/forwarded headers 构造，修复后 web healthy 且 smoke 验证 `127.0.0.1` origin。

## 停止、恢复与显式 reset

- `docker compose stop` / `docker compose start` 保留数据。
- 普通恢复先执行 `docker compose ps` 与 ready 检查，再按本文件启动入口恢复，不删除 volume。
- `scripts/docker-reset.sh --confirm askme-local` 会删除 Askme 本地容器和两个命名 volume，属于显式 from-zero 操作；未获得清空授权时禁止执行。
