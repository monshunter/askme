# OP-010：PLAN-026 Query-understood RAG V4 重建、部署与真实验收

状态：`verified`

关联 Plan：[PLAN-026](../plans/PLAN-026.md)

关联 Spec：[SPEC-002](../specs/SPEC-002.md)

关联 Design：[DESIGN-005](../architecture/DESIGN-005.md)

适用环境：本机 Docker Compose `askme-local`

验收日期：2026-08-15

## 数据备份、Knowledge 重组与索引状态

- 维护前在 PostgreSQL 18.6 容器内生成 custom dump，并复制到 `/tmp/askme-pre-query-understood-rag-v4.dump`；文件为 `6,145,512 bytes`，`pg_restore -l` 可以读取全部 `370` 个 TOC Entry。
- 维护前计数为 `users=4 / materials=4 / repositories=3 / publications=9 / conversations=50 / messages=254 / knowledge_items=32 / active_sources=92 / active_children=417 / active_indexes=1`，ingestion 与 RAG source 有效租约均为 `0`。
- 停止本项目 worker 后，先在 Compose 网络内执行维护入口 dry-run，再执行 `npm run rag:rebuild -- --execute --activate`。4 份已索引 Material 均重新完成 Knowledge organization；最终 `knowledge_items=37 / entity_values=113`，Material job 全部为 `completed`。
- 维护入口返回索引版本 `388863ea-4cf2-40e6-8419-3e60d3571bf7`。当前变更只调整 Query/Answer runtime，不改变 embedding/chunking fingerprint，因此 coordinator 安全复用已是 active 的 `source-entity-context-v2` 版本，`indexedSources=0`；92 个 source 与 417 个 child 保持 active，没有重复 embedding 或切换到不完整 index。旧 `source-context-v1` 继续为 `superseded`。
- 最终计数为 `users=4 / materials=4 / repositories=3 / publications=9 / conversations=61 / messages=364 / knowledge_items=37 / active_sources=92 / active_children=417 / active_indexes=1`，有效 RAG source 租约仍为 `0`。相较维护前新增的 11 个 Conversation 与 110 条 Message 来自本次反复执行的隔离 API/浏览器验收及一次失败诊断；Runtime 评测不持久化会话。账号、Material、Repository、Publication、原会话和授权事实没有被维护删除。

## 部署与运行状态

- 使用 `scripts/docker-up.sh -d` 保留 PostgreSQL、uploads 与 Repository Artifact volume；没有执行 reset 或删除 volume。最终 migrate exit 0、Web healthy、worker running，容器内 `ASKME_RAG_POLICY_VERSION=query-understood-rag-v4`。
- `/api/health/live` 为 `live`，`/api/health/ready` 顶层为 `ready`，database、migration、worker 为 `ready`，AI 为 `configured`。最终 web/worker 日志只有启动记录，没有 error 或 warning。
- Ready 中 Code Agent 的 runner stale、artifact degraded、BoxLite unavailable 与 provenance unverified 是本次开始前已存在的独立能力状态；普通 RAG、Query Understanding、Citation 与 Public Chat 不依赖它，本记录不把该状态描述为恢复。

## Query、检索与真实 Provider 评测

- Query Semantics 成对集 `20 / 20 PASS`；`requiredRoleFalsePositive=0`、`requiredEntityMiss=0`、`discoveryFalseNone=0`、`entitySubstitution=0`。覆盖无实体经历/项目/技能、显式已知和未知实体、profile owner、时间范围、待求字段、incidental context、clarify、Provider/Host seed 冲突与 fallback。
- 核心评测 `120` 个 synthetic retrieval case 的 `initialRecall@30=1`、`rerankRecall@8=1`、permission refusal rate `=1`，visibility/forbidden selection leak `=0`；另有 `12 / 12` entity-grounding 与 `10 / 10` entity-query case 通过。该组明确不声称数据库 SQL、Provider outcome 或 Citation precision。
- Compose 网络中的真实 PostgreSQL/Query Agent/Embedding/Rerank/Answer/Verifier 评测为 `26 / 26 PASS`：24 条覆盖已知/未知实体、多实体、唯一/歧义/缺失指代、无实体时间经历、项目/技能发现、incidental mention 与富途聚焦；2 条注入 Embedding/Rerank 故障并验证安全降级仍返回有 Citation 的 Askme 回答。
- 首轮真实运行时评测发现四路共用 SQL 把 `rag_source_kind` 与 `text[]` 比较，PostgreSQL 拒绝执行；已修正为 `rag_source_kind[]` 并补定向测试。随后评测又发现旧 Askme/Repository fixture 与新增简历 fixture 属于两个真实 Candidate，已改为显式 `entity/profile` dataset 和两个 owner，避免用错误 tenant 制造假阴性或伪造资料；同一最终评测全量通过。
- 稳定性复验曾发现“看过 Askme 后，我还做过哪些项目？”在召回 54–57 条候选后仍间歇性整体拒答。根因是 Answerability 未收到 mention 的 `context` 角色，把附带实体扩大成覆盖条件；修订后该题连续 `5 / 5 PASS`，并在最终 26 题矩阵中再次通过。
- HTTP 复验又发现“Askme 和 MoonBase 分别解决了什么问题？”在 Askme 已解析、MoonBase 缺失时偶发把整个共享 aspect 判为 unsupported。Answerability Packet 现在明确区分 `requiredResolution.resolved` 与 `requiredResolution.unavailable`：前者必须取证，后者由 Host 作为 partial gap 单独呈现；该题连续 `5 / 5 PASS`，最终 Runtime 与 HTTP 全量复验均通过。

## HTTP 与持久化 Trace Evidence

- Compose 网络中的真实 HTTP 评测为 `16 / 16 PASS`：Candidate 7 条、默认 Public 6 条、简历 Public 3 条；共有 11 条 grounded answer、5 条 deterministic failed-close，未知源码问题新增 Deep Run 为 `0`，Public Citation 不暴露内部 chunk ID。
- Candidate 与默认 Public 覆盖 Askme 显式命中、可信上下文指代、无实体项目发现、多实体 partial、歧义指代、未知实体和未知源码；简历 Public 覆盖原始 2022–2024 时间问题、`看过 Askme 后…` incidental context 和富途 required organization。
- 全部 Trace 使用 `query-understood-rag-v4`。浏览器原始时间问题的持久 Trace 为 `intent=employment_history / subject=profile_owner / queryMode=discovery / requestedFields=[company,job_title,responsibilities] / required mentions=0 / coverage=full / selected evidence=1`；incidental 问题为 `queryMode=discovery`，Askme mention 记录为 `role=context`，未形成 source hard scope。

## 真实浏览器 Evidence

- Candidate `/workspace/agent` 的无实体项目问题返回 copybook 与 new-api，并显示 4 个 Citation；同一 Candidate 没有简历证据，对 2022–2024 任职问题正确显示“需要更多证据 / 未暴露来源”，没有借其他 Candidate 的富途资料形成假阳性。刷新后问题和结果保留，Console error/warning 为 `0`。
- 公开简历 Agent `/a/tLfbE3_qnKP00a3Md98HwEBocO_uuWLD` 对目标问题返回 `2022年4月至2024年10月 / 富途控股 / 云原生平台工程师`，并回答 Kubernetes 集群运维、服务治理、命名服务核心组件和平台标准化职责，显示 1 个公开 Material Citation。
- 同一公开会话继续询问“看过 Askme 后，我还做过哪些项目？”，返回 Kubernetes 命名服务与 EasyInterview 等项目并显示 1 个公开 Citation，没有把不存在于该 owner Catalog 的 Askme 当作 Required Entity。刷新后两轮问题、答案与两个 Citation 均恢复，Console error/warning 为 `0`。
- 公开页面在 `390×844` 视口下 `clientWidth=scrollWidth=bodyScrollWidth=390`，没有横向溢出，输入框、目标问题和 Citation 均可见；验收结束后已恢复默认 viewport。

## 最终自动化门禁与边界

- Vitest `105 files / 423 tests`、ESLint、Next typegen + TypeScript、production Build `31 / 31`、Surface Matrix `22 pages / 68 API routes / 76 methods / 29 verification entrypoints` 与 `git diff --check` 全部通过。
- 本记录证明当前本机 Compose、当前两套真实 Candidate 数据、真实 Provider、Candidate Preview runtime/API/UI 和 Public Chat 可用；不外推为生产容量、开放语言数学零错误、外部 Chrome 可用或独立 Code Agent 能力恢复证明。
