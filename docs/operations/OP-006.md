# OP-006：PLAN-020 Hybrid Agentic RAG V2 本地部署与真实验收

状态：`verified`

关联 Plan：[PLAN-020](../plans/PLAN-020.md)
适用环境：本机 Docker Compose `askme-local`
验收日期：2026-08-14

## 部署前事实与恢复准备

- 部署前业务数据为 `users=4 / materials=4 / knowledge_items=39 / repositories=2 / conversations=32 / messages=152 / schema_migrations=20`。
- 部署前使用 `pg_dump` 生成 `/tmp/askme-pre-hybrid-rag-v2.dump`，文件大小为 `448382 bytes`。该文件是本轮本机恢复资产，不是生产备份或长期异地备份。
- PostgreSQL 镜像固定为 `pgvector/pgvector:pg18@sha256:2ba9ca5f2e7daa0f0e7723cba1ee9167bab54efd3640516a44ac1a928dd67e7a`；migration `0021_hybrid_rag_v2.sql` 只新增扩展和派生 RAG 表，不删除原业务表、列或数据。
- 回滚路径是停止继续写入、保留当前 volume、使用部署前 dump 恢复业务库并回退应用 revision。应用重部署均使用 `scripts/docker-up.sh -d`，未执行 volume reset。

## 部署、Provider 与索引 Evidence

- 本地数据库为 PostgreSQL 18.6，`vector=0.8.6`、`pg_trgm=1.6`；当前唯一 active index 为 `qwen3.7-text-embedding / 1024 dimensions / expected=92 / active=92`。
- Embedding 与 Rerank 使用独立配置；Rerank 以 `dashscope-compatible` 协议调用 `qwen3-rerank` 的 `/compatible-api/v1/reranks`，未复用 Chat endpoint 或 Secret。Web 容器中 Provider 配置为 ready。
- `npm run eval:rag-v2:provider` 实际调用独立 Rerank Provider 完成 120 题，Recall@30、Evidence Recall@8、Citation precision 和 outcome classification 均为 `1`；false none、hallucination、unauthorized leak 均为 `0`，`failures=[]`。
- 最终 active source 为 `92`，旧 OneCat Wiki projection 的 `8` 个 source 保留为 `superseded`；active index `expected_source_count=92`，没有 queued、processing、failed 或 revoked source。
- 目标账号 `monshunter/onecat` 固定 commit `eae3cd4a2a62b66dad157912794f91cee64a510b`，Repository 文档状态为 `ready / 10 indexed / 0 skipped`。最新 approved Wiki projection 含 `6` 个 active source；Repository readiness 只统计 Markdown/PDF，不混入 Wiki source。

## Code Agent 与 Repository 生命周期 Evidence

- Host-native OCI image 固定为 `sha256:a9527a3f4234388bd37e380dc04f01fc340a1f69da91faa416dd8266a9e40d8c`。`/api/health/ready` 的 runner、artifact、BoxLite 和 provenance 均为 `ready`。
- 目标账号首次成功分析 run `e7531903-3d9b-465b-9a1e-4270b82c55b1` 使用 `deepseek-v4-pro`，执行 `37 rounds / 68 tool calls / 30 examined files`，microVM 在提交前完成清理。第一次输出因 `WIKI_LINK_INVALID` 被 Host 拒绝，错误专用 correction 成功；Dossier `9ef26a98-5f6f-48f5-b21e-0389cc36f38e` 经真实 UI 批准。
- visibility 撤销正确取消 generation 2。恢复后的 generation 3 run `31acd60f-58b3-42e6-9d11-278f6248e0f8` 执行 `35 rounds / 79 tool calls / 28 examined files`，生成 Dossier `e100f6b1-de93-4df5-a3d4-3cbccc3636d0`；最新 projection `3f1d51fa-8ce7-4bcb-b864-f404feda5bf7` 已经真实 UI 批准，旧 projection 为 `superseded`。
- `smoke:code-agent-sandbox` 验证真实 microVM、只读工具、Repository 指令隔离、一次 correction、Secret 不持久化和清理；`smoke:repository-analysis-runner` 验证真实 lease、Host Citation 校验、Dossier/会话提交、immutable source preview 与 terminal completed。最终 `pending/running analysis_runs=0`。

## 自动化与 Golden Evidence

- `npm run lint`、`npm run typecheck`、`npm run build`、`git diff --check` PASS；Vitest 为 `100 files / 348 tests` PASS，`npm audit --audit-level=high` 为 `0 vulnerabilities`。
- Surface Matrix 为 `22 pages / 68 API routes / 75 methods / 28 verification entrypoints`；`smoke:admin` 验证 Retrieval Trace 角色边界、安全投影、治理状态机和审计。
- `smoke:rag-index-foundation` 在真实 PostgreSQL、真实 Embedding 与 worker 中验证 Markdown/PDF、四路召回、Citation、Trace、强撤销与清理；结束后 fixture user/repository 均为 `0`，active source 仍为 `92/92`。
- `npm run eval:rag-v2` 与 `npm run eval:rag-v2:provider` 都完成 3 名虚构 Candidate、120 题和全部语言、来源、问题类型、降级分段；两种模式全部阈值 PASS。

## 目标账号真实浏览器 Evidence

- 富途问题“在富途控股担任云原生平台工程师期间，你具体负责了哪些工作？”在最终 active index 上返回 Kubernetes 集群运维、服务治理和命名服务核心组件职责；消息 `588709f2-70bb-4f3e-88fc-b99e135c6591` 为 completed，Trace `rounds=1 / degradations=[]`，授权简历 Citation 可在内部阅读页重新打开到对应经历。
- 最终 Candidate Repository 问题“普通删除与 `--purge` 的恢复和不可逆边界”返回 4 个固定 revision 来源；消息 `8f8d658b-2280-497a-9737-aa69658a4304` 为 completed，Trace `rounds=1 / degradations=[]`。Citation 打开后显示完整 commit、path `docs/spec/onecat-http-gateway-v1.md` 和 `620–679` 行正文。
- 最终 Public 问题“`--purge` 为什么需要独立明确授权”返回 2 个公开 Citation；消息 `05049ed5-80e3-4b4c-befa-89ff6437db4d` 为 completed，Trace `rounds=1 / degradations=[]`。Public Citation 同样重新授权并打开到固定 commit 和准确行范围。
- Repository 从 `public_preview` 降为 `private` 时，Candidate 历史消息 `b56dc915-0caa-463c-ab38-b881ab10b2b8` 与 Public 历史消息 `f7e16a25-ae63-4731-ac8c-9f0163249a7f` 在同一事务中持久失效，source version 立即 revoked；UI 不显示 Citation。恢复 `public_preview` 并重新索引后，两条旧回答仍显示“来源权限已变化”且 Citation 为空，新问题才使用当前 source。
- Candidate 与 Public 最终页面 console logs 均为 `[]`。浏览器控制插件自身的 Statsig 网络超时不属于 Askme 页面 console/network 结果。

## 最终数据与运行状态

- 最终业务数据为 `users=4 / materials=4 / knowledge_items=39 / repositories=3 / conversations=33 / messages=166 / schema_migrations=21`。相对部署前只保留目标账号 OneCat Repository、一个 Public 会话和本轮 7 组真实问答；没有 Smoke fixture。
- 为目标账号验收创建的一次性 session `5049d556-671e-4118-b48c-4bef62fd26ce` 已精确撤销，并记录 `auth.e2e_session.cleanup` 审计；未影响该账号其他 session。
- `/api/health/live` 为 `live`；`/api/health/ready` 总体为 `ready`，database、migration、worker、runner、artifact、BoxLite、provenance 均为 `ready`，AI 为 `configured`。Web、db、mailpit healthy，worker 与独立 runner 持续运行。
- 本记录证明本机 Docker Compose、真实数据库、真实 Embedding/Rerank/Chat/Verifier、真实 BoxLite/Code Agent 与目标账号浏览器链路；不外推为生产发布、生产容量或 OCR 验收。
