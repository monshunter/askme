# OP-008：PLAN-024 公共 Agent 相关性修复部署与真实验收

状态：`verified`

关联 Plan：[PLAN-024](../plans/PLAN-024.md)
适用环境：本机 Docker Compose `askme-local`
验收日期：2026-08-15

## 变更与部署边界

- 本次只修改 Hybrid RAG Coverage 和 RAG 证据不足后的 Repository fallback，并同步 Public Chat 与 Candidate Preview 两个 consumer；不改变来源权限、索引结构、模型 Provider、Secret、数据库 schema、UI 或生产环境。
- 部署使用 `scripts/docker-up.sh -d` 重建 `migrate/web/worker`，没有 reset PostgreSQL、uploads 或 Repository Artifact。
- 最终镜像为 migrate `3a493708497406439d4a24d8e1220d0f26c731995daf2e2ffbc57cb6c9c8e40d`、web `9ec297411760ab0d166832cbe7068826fc323b072d5f07e6a6adfc55638c160b`、worker `710ec7254f9ded4b3d8b4ea43d3e7f7af58be5a653e9b3797dfdf133d17abe07`。

## 数据、健康与清理 Evidence

- 部署前业务计数为 `users=4 / materials=4 / knowledge_items=39 / repositories=3 / conversations=40 / messages=192 / rag_source_versions=101 / schema_migrations=21`；最终为 `4 / 4 / 39 / 3 / 49 / 218 / 101 / 21`。新增的 9 个 Conversation 和 26 条 Message 全部来自本 Plan 的多轮 Reconcile、API 与浏览器验收，其他业务数据计数未变化。
- `askme_local_pgdata` 和 `askme_local_uploads` 创建时间始终为 `2026-08-10T14:54:04Z` 与 `2026-08-10T17:18:17Z`，证明既有 volume 未被替换。
- 最终 migrate exit 0、Web healthy、worker running，部署后 web/worker/migrate 的 error、exception、fatal、panic、unhandled、warn 日志为空；active Analysis Run 为 0。
- 中途旧 fallback 产生过一条与 `askme 项目呢？` 测试消息精确关联的 pending Deep run。它已按现有取消状态机写入 `CODE_AGENT_CANCEL_REQUESTED`，最终为 `cancelled / CODE_AGENT_CANCELLED`，对应 pending 消息为 `DEEP_ANALYSIS_CANCELLED`，没有删除会话或审计记录。
- `/api/health/ready` 顶层为 `ready`，database、migration、worker 为 `ready`，AI 为 `configured`。host-native Runner 在本次开始前已 stale，因此 Code Agent capability、artifact、BoxLite、provenance 仍为 degraded/unavailable/unverified；本次 Public RAG 修复不依赖该能力，也未把既有运行状态冒充为本次回归。

## 最终真实 API Evidence

最终镜像部署后，以公开 Agent `牛鼻子`（slug `tLfbE3_qnKP00a3Md98HwEBocO_uuWLD`）和当前 `x-askme-visitor-token` 合同新建两个真实游客 Conversation：

- `b0e8e1b6-db5a-4458-8062-4cd3c1f1f37d` 直问 `askme 项目呢？`：HTTP 200，`INSUFFICIENT_EVIDENCE`，回答“当前没有足够的授权证据来准确回答这个问题。”，0 Citation；Trace 为 `coverage=none / roundCount=2`。
- `ed1663ed-542a-4767-93a2-fbbebba73ffb` 先问 `onecat 是什么项目？`：HTTP 200、无 error code、回答包含 OneCat、公开响应有 6 个 Citation；随后问 `askme 项目呢？`：HTTP 200，`INSUFFICIENT_EVIDENCE`，同一证据不足文案，0 Citation，Trace 为 `none / 2`。
- 两个 Askme 路由审计都没有第二条 Deep fallback，数据库最终没有 pending/running Analysis Run。

## 最终真实浏览器 Evidence

- 在最终镜像的 `/a/tLfbE3_qnKP00a3Md98HwEBocO_uuWLD` 新建会话，先问 OneCat 后追问 Askme。页面先显示 OneCat 三点回答、6 个公开来源，再显示 Askme 的证据不足文案和“需要更多证据”状态，不显示 Askme Citation。
- 刷新后该会话仍以 4 条消息存在于历史列表，OneCat 来源和 Askme 证据不足结果都保留；对应 Conversation 为 `f3e70729-c073-4544-8f7f-c42985ee436f`。
- 页面 `clientWidth=1280 / scrollWidth=1280`，无横向溢出；浏览器 Console warning/error 为 0。独立真实 HTTP 链路已经覆盖三个 Chat POST 的 200 状态和响应字段。

## 自动化与已知边界

- 最终 Diff 通过 Vitest `101 files / 364 tests`、ESLint、Next typegen + TypeScript、production Build `31 / 31`、Surface Matrix `22 pages / 68 API routes / 76 methods / 29 verification entrypoints` 与 `git diff --check`。
- 额外执行现有 `smoke:public-chat` 时，宿主第一次因未导出 `DATABASE_URL` 未进入测试；改在 Web 运行时执行后，fixture 在首个 Orion 回答处得到 `INSUFFICIENT_EVIDENCE`。该脚本只写旧 `chunks`，没有创建 V2 `rag_source_versions/rag_evidence`；父提交在 `evidence=[]` 时同样返回 `none`，且 fixture 没有 Repository，因此这是既有 V1 fixture 漂移，未作为本 Plan 的通过 Evidence，也未在本次授权内改写。
- 本记录只证明本机 Compose、真实“牛鼻子”Public Chat API、真实浏览器、会话持久化与 RAG/Router Host 边界，不外推为生产发布或 host-native Code Agent 恢复。
