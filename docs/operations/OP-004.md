# OP-004：PLAN-004 Platform Admin Docker 与 Chrome E2E

状态：`Completed`

关联 Plan：[PLAN-004](../plans/PLAN-004.md)
适用环境：本机 `askme-local` Docker Compose project 与用户 Chrome

## 已通过的运行结果

- migration `0011_admin_governance.sql` 已应用；Docker production build 完成 Next.js 编译、类型检查与 Admin route 生成，Web、worker 和 migration 使用同一镜像链路。
- 当前 `/api/health/ready` 返回 database、migration、worker `ready`，AI `configured`；worker 使用 `deepseek-v4-flash`。
- `smoke:admin` 在当前 Docker HTTP 服务与 PostgreSQL 上通过匿名 `401`、Candidate `403`、真实聚合、安全投影、Candidate session 撤销、Candidate/Agent 公共访问即时暂停与恢复、Content Review 状态机、平台策略持久化、七类治理审计和 SMTP 未配置显式 `409`。
- 自动化门禁为 37 test files / 125 tests PASS；`npm run lint`、`npm run typecheck` 与 `git diff --check` PASS。
- 专用 Chrome fixture 已通过白名单 ID 清理；Candidate、publication、Content Flag 和其目标审计记录均未留在当前数据库。

## Chrome 已通过场景

- 当前真实桌面内容视口为 `1920 × 912`；Admin Overview 的侧栏宽度为 272px，五张指标卡均渲染，`scrollWidth=innerWidth=1920`，无横向溢出。
- Candidate 搜索只返回构造的持久账号；提交 suspend 后治理对话框自动关闭，公共 Agent 立即显示 unavailable；restore 后旧 session 不恢复且公共 Agent 重新可访问。
- Published Agent 搜索、pause、restore 均完成；公共页在 pause 后立即 unavailable，restore 后恢复原不透明链接。治理对话框在成功或取消后默认关闭。
- Content Review 仅展示 `safe_summary`，高风险项完成 `open → reviewing → resolved`，低风险项完成 `open → dismissed`；决定说明和状态刷新均来自 API/数据库。
- Settings 展示 PostgreSQL、migration、worker 与 DeepSeek 实时状态；SMTP 未配置时邀请表单不可用且没有伪造成功。保存当前策略后页面确认策略已持久化并治理新公共请求。
- Reports 从 7 天切换到 30 天后展示真实 UTC buckets 与 persisted distributions；全局搜索以 `6ac92c41` 同时返回 Candidate、Published Agent 和 Content Review 安全摘要。
- Admin 与公共 Agent 专用 Chrome 标签的 console 最终均为空；修复了服务端与浏览器隐式 locale/timezone 不同导致的 React hydration error。

## Responsive 验收 Reconcile

此前 Chrome 插件没有直接 viewport resize 能力，且专用标签在复核前被关闭，导致本记录进入 Waiting；未通过间接页面构造、替代浏览器或原始 CDP 伪造验收。

用户于 2026-08-11 明确授权 Agent 自行通过 Computer Use 打开 Chrome DevTools，并指定移动验收直接选择 `iPhone 14 Pro Max`（430 × 932）。该指令替代旧的手工 `390 × 844` 恢复条件；桌面仍使用 1448 × 1086 响应式 viewport。

## 精确视口终验

- 可见 Chrome DevTools 已直接选择 `iPhone 14 Pro Max`，toolbar 与页面运行时同时证明 `innerWidth=430`、`innerHeight=932`、`devicePixelRatio=3`；Overview 和 Settings 的 document/body `scrollWidth=430`，`horizontalOverflow=false`。
- 移动抽屉可展开全部六个 Admin 入口，进入 Candidates 后自动收起；Candidate 表格按字段标签重排，搜索、状态筛选、分页和治理动作仍可达。Suspend 对话框要求审计原因，空理由时确认禁用；本次用 Cancel 关闭且未改变本地 Candidate。
- Published Agents、Reports、Content Review 与 Settings 均在同一 430 × 932 档位加载真实搜索、筛选、时间范围、策略和空态；Settings 明确显示 SMTP 未配置，不伪造邀请成功。
- Chrome viewport capability 在专用验收标签上设置 `1448 × 1086`，Overview 实测 document/body `scrollWidth=1448`、`horizontalOverflow=false`；侧栏、顶部栏、五项指标、最近发布、Review、增长图和 Quick Actions 同屏保持设计稿层次。验收后已 reset 回默认 viewport。
- 初次可见 DevTools 检查发现 Askme 自身请求 `/frontend_bg_left.png` 返回 404；根因为同一 CSS 内两处使用未交付的绝对 public URL。改为与登录页一致的相对打包资源后重新 production build、替换 Web 容器并复验，Askme 资源错误消失。
- 修复后的 Chrome 插件 `warning/error` 日志为 `[]`。可见用户 Chrome DevTools 仅剩三个已安装扩展产生的错误（AdBlock 的不可访问资源、无效 extension URL、Immersive Translate 版本不匹配），没有 Askme URL、资源、React 或脚本错误。
- 可见 DevTools 最终恢复并保留为 `iPhone 14 Pro Max`（430 × 932，97% fit），作为后续移动验收默认档位。
