# OP-002：PLAN-002 Candidate Docker E2E

状态：`Completed`

关联 Plan：[PLAN-002](../plans/PLAN-002.md)
适用环境：本机 `askme-local` Docker Compose project 与用户明确指定的 Chrome

## 运行结果

- db、web、worker 当前运行，db/web healthy；migrate 已以退出码 0 完成。
- Docker Candidate GitHub Connector 已真实导入 `sindresorhus/p-limit`，worker 使用 `deepseek-v4-flash` 将其组织为 4 个可搜索 Knowledge Item，并写入精确的 Knowledge Item ↔ Evidence 关系；详情显示 `Sources (1)` 与 `Evidence (5)`。
- Chrome 桌面场景已验证 Dashboard、Upload Materials、Knowledge Base 的真实数据、失败态、搜索、详情和删除；1448 × 1086 下侧栏 272 px、顶栏 94 px，三页均无横向溢出，console error 为 0。
- Chrome 移动场景已验证 390 × 844 下三页无横向溢出，表单与资料/知识状态可读，移动导航跳转后自动关闭，console error 为 0。
- Upload Materials 应用内确认框已验证 Cancel 保留资料、Delete 清理资料；删除后的 `concurrency limiting` 搜索返回 0 条。
- 删除后数据库为 `materials=3`、`knowledge_items=20`、孤儿 Knowledge Item 为 0；上传卷文件与三条保留资料逐一对应。
- 生产镜像已修复 standalone 静态资源 404、SSR 时间水合差异、Connector 异步 form 引用和应用内删除确认框。
- 当前 `npm test` 为 15 files / 65 tests PASS；`npm run typecheck`（含 `next typegen`）、`npm run lint`、`npm audit --audit-level=moderate`、生产 Docker `npm run build` PASS。
- Docker lifecycle smoke 已验证 owner 隔离、失败重试、删除文件与派生知识；当前数据库 `knowledge_items=20`、孤儿 Knowledge Item 为 0。
- 重建后的生产 API 对非法 material delete/retry 与 knowledge detail 路径稳定返回 404；独立 worker heartbeat 在真实数据库持续约每 10 秒刷新，ready 保持 `worker=ready`。

## 已解决的恢复事件

旧版 Askme Chrome 标签页曾持有一次已经移除的原生 `window.confirm`，阻塞了 Chrome 扩展。用户于 2026-08-11 关闭该弹窗，并明确允许后续本地验收默认关闭此类原生弹窗。恢复后新标签页确认 `getJsDialog()` 为空，当前应用内删除确认框与完整场景均已通过。

## 结论

PLAN-002 4.1 与 5.2 的 Docker + Chrome Evidence 已成立，可以进入 Change Review。
