# OP-007：PLAN-023 DeepSeek Flash 降本部署与 LLM 核心链路回归

状态：`verified`

关联 Plan：[PLAN-023](../plans/PLAN-023.md)
适用环境：本机 Docker Compose `askme-local` 与 host-native Agent Runner
验收日期：2026-08-14

## 变更与部署边界

- 当前生效的 `ASKME_AI_CODE_MODEL` 默认值从 `deepseek-v4-pro` 改为 `deepseek-v4-flash`；Router、RAG、Planner、Verifier 原本已使用 `deepseek-v4-flash`。Code profile 的 `thinking=high`、`contextWindow=1000000`、`maxTokens=200000` 和其他预算未改变。
- 生效配置、默认配置、测试与 Smoke fixture 已统一；全仓剩余的 `deepseek-v4-pro` 只存在于本 Plan/Review 的变更说明和 [OP-006](OP-006.md) 的历史真实运行记录，不再有可选择该模型的有效路径。
- 部署仅使用 `scripts/docker-up.sh -d` 重建和替换 `migrate/web/worker`；未 reset PostgreSQL、uploads 或 Repository Artifact，未修改 AI API key、base URL、Embedding、Rerank、权限或生产环境。

## 部署与恢复 Evidence

- 部署前镜像为 migrate `99fb0fd077cb`、web `85ac6db8d85f`、worker `3307fda518f9`；部署后为 migrate `ff429c8f006a`、web `0085f7349a18`、worker `0c4f3dcdaf1f`。旧镜像 ID 是本机应用层回退点，数据 volume 未变化。
- 部署前后业务计数均为 `users=4 / materials=4 / knowledge_items=39 / repositories=3 / conversations=36 / messages=176 / rag_source_versions=100 / schema_migrations=21`；`askme_local_pgdata` 与 `askme_local_uploads` 的创建时间仍为 `2026-08-10T14:54:04Z` 和 `2026-08-10T17:18:17Z`。
- migrate exit 0，Web healthy，worker running；host-native Runner 通过 `scripts/agent-runner.sh` 恢复，PID `79888`。最终 live 为 `live`，ready 顶层与 database、migration、worker、runner、artifact、BoxLite、provenance 均为 `ready`，AI 为 `configured`，Code Agent capability 为 `ready`。
- Web 与 worker 容器中的 Router、RAG、Code、Planner、Verifier 五个实际模型环境变量全部为 `deepseek-v4-flash`；部署后 Web/worker/migrate 错误日志为空，`data/boxlite/boxes` 为 0。

## 真实 LLM、RAG 与 Agent Evidence

- 已部署 Web 容器内 `npm run ai:check` 真实调用 DeepSeek 成功，事件为 `ai.check.completed`，模型 `deepseek-v4-flash`，延迟 `728 ms`。
- 资料处理使用 490 B Markdown fixture 完成 queued → indexed，生成 2 个 Knowledge Item；`material.organize` usage 为 `deepseek-v4-flash / success / 340 input / 254 output`，RAG source 曾达到 active `1 parent / 1 child / 144 tokens`。验收后 Material 与派生 Knowledge Source 已通过真实 UI 删除。
- `npm run eval:rag-answer-quality` 在已部署 Web 容器真实调用 RAG Generator 与 Verifier，当前日期 `2026-08-14` 的 2 个回答质量用例全部 PASS，两个答案各有 1 个 Citation。
- `npm run smoke:agent-runtime-acceptance` 最终 PASS。Candidate RAG 对“copybook 是一个什么样的项目？”返回中文答案并引用固定 revision `10abc90f0d244485c0983a79f0c79238671bd3f0` 的 `README.zh-CN.md` 与 `AGENTS.md`；Router、RAG answer 与 LLM 推荐问题 usage 均记录为 `deepseek-v4-flash / success`。
- Candidate Deep run `b8b32d59-8bce-4d10-9b3a-3ad638219388` 与 Public Deep run `6c97e5bd-4adc-4117-993f-678ccf620be2` 均为 `completed`，configured/actual model 都是 `deepseek-v4-flash`，safe error 为空且 cleanup timestamp 已写入。Candidate run 使用 `6 rounds / 11 tools / 7836 input / 4661 output`，Public run 使用 `8 rounds / 13 tools / 6336 input / 4089 output`；两者都引用 `src/lib/pagination.ts` 及测试，count quota 未变化，路由审计为 Deep。
- Runner 对两个 Deep run 的初始结果均观测到一次 `CODE_ANSWER_CITATION_HASH_INVALID` Host 拒绝，随后在原预算内收敛为 `completed/answered`；最终 Citation、message、run terminal state 与 microVM cleanup 一致，没有把失败初稿写成用户答案。

## 真实浏览器 Evidence

- Candidate `/workspace/agent` 可见本轮 RAG 概览回答与 Deep `paginate` 回答；Deep 回答解释剩余格子形成较短最后一页，并显示 5 个固定 revision 源码/测试引用。
- Public Agent 新建独立会话后完成同一概览问题的真实 RAG 回答，显示 2 个公开引用 `README.zh-CN.md:1-100` 与 `docs/spec/architecture-design.md:1-74`，推荐问题随回答更新。
- 浏览器页面 `clientWidth=1280 / scrollWidth=1280`，Candidate/Public/资料清理过程中 console warning/error 均为空。浏览器临时公开会话和上传 fixture 已通过产品确认流程精确删除。

## 自动化、清理与剩余边界

- 当前最终 Diff 通过 Vitest `101 files / 360 tests`、ESLint、Next typegen + TypeScript、production build `31 / 31`、Surface Matrix `22 pages / 68 API routes / 76 methods / 29 verification entrypoints` 和 `git diff --check`。
- 三次 Acceptance 尝试共保留 4 个真实验收 Conversation 和 10 条 Message，因此最终为 `conversations=40 / messages=186`；这些记录用于复核 Flash 回答。其他原业务计数未改变。
- Material 删除链路现有实现会让已删除 Material 的 RAG source 暂留 `active` 状态，但检索与历史 Citation 都因 Material 不存在而 fail closed。本轮 fixture 已按精确 source id 撤销并重算 active index，最终 `expected_source_count=92 / active=92`；这是本次模型切换前已存在的生命周期缺口，不由本变更引入，产品代码修复不在 PLAN-023 授权范围。
- 本记录证明本机 Compose、真实 DeepSeek、Embedding/Rerank、RAG、Candidate/Public Agent、Code Agent、BoxLite 与浏览器链路；不外推为生产发布、模型容量或长期成本账单验证。
