# OP-005：PLAN-006 本地运行、可观测性与总验收

状态：`verified`

关联 Plan：[PLAN-006](../plans/PLAN-006.md)
适用环境：本机 Docker Compose `askme-local` 与隔离 `askme-acceptance-*` project
验收日期：2026-08-11

## 生命周期与恢复 Evidence

- `npm run verify:docker-lifecycle` 使用唯一 project `askme-acceptance-1786449356-23539`、随机宿主端口和两个全新 volume，从空数据库完成 migration、Candidate/Admin bootstrap、Web healthy、worker ready 与 DeepSeek configured。
- 真实 Candidate 登录、上传、worker ingestion 与 AI organization 创建 material、completed job、1 条 Knowledge Item、request ID 审计和上传文件；同步 restart db/web/worker 后，同一 session 仍可访问，material、knowledge、job ID、request audit、文件路径和 checksum 全部一致。
- 脚本证明 `scripts/docker-reset.sh` 对缺少确认或错误 project 均拒绝执行；验收结束仅删除本轮隔离容器、network 和两个 volume，当前 `askme-local` 未被停止、重启或清空。
- 随机宿主端口在 restart 后可能重新分配，验证入口会重新解析 Compose port，不复用旧 endpoint。

## 可观测性与日志安全 Evidence

- JSON API、健康响应和登录/注销重定向统一回写安全 `x-request-id`；JSON envelope 与 header 使用同一 ID，非法上游 ID 被替换为 UUID。
- 关键 Candidate、发布、聊天与 Admin 操作继续写入包含时间、结果和 request ID 的 `audit_events`；worker 结构化日志使用 worker/job/material ID 与 outcome/error code。
- API 5xx 定向测试证明日志不含 Error message 或私有正文；隔离 restart 日志对数据库、SMTP、Candidate/Admin、DeepSeek Secret 和完整私有 fixture 正文的扫描均无匹配。
- 未处理的 `pg.Pool` error 已收敛为字段白名单事件，详见 [BUG-003](../bugs/BUG-003.md)。

## 当前 revision 总门禁

- `npm test`：41 test files / 138 tests PASS。
- `npm run typecheck`、`npm run lint`、`npm audit --audit-level=moderate` PASS；audit 为 0 vulnerabilities。
- 隔离 Docker production build 完成 Next.js compile、TypeScript、25 个动态路由生成与三服务镜像导出；当前 `askme-local` 使用同一 build cache 无损重建。
- migration/bootstrap 重入 PASS，`askme-local` db/web healthy、worker running；ready 返回 database/migration/worker ready、AI configured，body/header request ID 一致。
- 当前 Docker HTTP + PostgreSQL 上以下 smoke 全部 PASS：auth、visibility retrieval、privacy API、publication、GitHub/Website connector、material lifecycle、agent preview、public chat、Admin、upload、job lease recovery。

## Chrome 总验收

- Candidate 7 个主界面在 1448 × 1086 与 `iPhone 14 Pro Max` 430 × 932 下逐页加载，`scrollWidth` 分别等于 1448/430，无横向溢出、alert 或新 console error。
- Candidate 通过真实 UI 开启 Public Mode、确认 privacy、生成不透明 link 并发布；公开 Agent 在 430 × 932 下完成真实 DeepSeek 问答，得到 1 条 completed assistant message 和 1 个 Citation source card，无横向溢出或新 console error。
- Admin 7 个主界面在 1448 × 1086 与 430 × 932 下逐页加载，无横向溢出、alert 或新 console error；最终 Chrome viewport 保留为 430 × 932。
- 浏览器 fixture 已 cleanup，`askme-browser-e2e@local.invalid` 及其级联 publication/session/material/knowledge 不保留。

## 当前状态与边界

- `askme-local` 数据与上传 volume 保留；Web 暴露 `127.0.0.1:3000`，PostgreSQL 暴露 `127.0.0.1:55432`。
- 本记录证明本地 Docker 交付，不声明生产部署、外部日志平台、生产备份或互联网发布就绪。
