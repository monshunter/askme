# SCN-002：Askme 全站页面、API、功能与 Repository Code Agent 真实回归

状态：`verified`

关联 Plan：[PLAN-014 Phase 7](../plans/PLAN-014.md)、[PLAN-017 Phase 5](../plans/PLAN-017.md)

关联验收：[SPEC-001](../specs/SPEC-001.md)、[SPEC-002 当前验收 Checklist](../specs/SPEC-002.md)

## 用户结果

在保留本地既有数据的前提下，运行 PostgreSQL、Web、文档 worker、host-native Agent Runner、Artifact Store 与每 run 新建的 BoxLite microVM，对 Askme 当前全部页面、用户功能、Route Handler method、正式 Scenario 和 package smoke/E2E 入口执行一次可复核的真实回归。任何 surface 未执行、只由低层测试间接覆盖或仅凭历史 Evidence 推断时，本场景都不能标记为 `verified`。

## 可执行 Surface Owner

`scripts/verify-surface-matrix.ts` 是本场景的精确 surface manifest 与漂移门禁，当前验证结果为：

- 页面：22；
- Route Handler：64 个路径、70 个 HTTP method；
- package 验证入口：27；
- 浏览器角色：anonymous、Candidate、Platform Admin；
- 浏览器 viewport：desktop、mobile。

每个页面都声明角色、桌面/移动 viewport 和至少一个自动化或浏览器 Evidence owner；每个 API method 都声明至少一个 smoke 或浏览器 Evidence owner。新增、删除或修改页面、Route Handler method、smoke/E2E/lifecycle script 时，`npm run verify:surface-matrix` 必须先失败并要求更新本场景，而不是静默漏测。

## 页面矩阵

| 角色 | 页面 | 必须验证 |
|---|---|---|
| Anonymous | `/`、`/login`、`/register`、`/forgot-password`、`/reset-password/[token]`、`/invite/[token]`、`/a/[slug]` | SSR、注册与密码找回、未登录边界、有效/无效 token 或 slug、语言切换、桌面/移动、console、network、overflow |
| Candidate | `/workspace`、`/workspace/account`、`/workspace/materials`、`/workspace/repositories`、`/workspace/knowledge`、`/workspace/privacy`、`/workspace/agent` | 登录、改密、导航、资料/Repository/知识/隐私/Agent 全功能、成功/空/等待/失败/撤权、桌面/移动 |
| Platform Admin | `/admin`、`/admin/candidates`、`/admin/agents`、`/admin/repositories`、`/admin/reports`、`/admin/reviews`、`/admin/search`、`/admin/settings` | Admin-only、搜索/筛选/分页、账号/发布/Repository/run 治理、健康/usage、安全投影、桌面/移动 |

## API 矩阵

精确路径、method 与 Evidence owner 由 `scripts/verify-surface-matrix.ts` 保存；下面按系统边界汇总执行要求：

| 边界 | 路径数 | 必须验证 |
|---|---:|---|
| Health | 2 | live、当前 migration、database/worker 主 readiness、runner/Artifact/BoxLite/AI 降级与恢复 |
| Auth / Locale / Invitation | 9 | 匿名/角色边界、注册、登录/登出/session、忘记/重置/变更密码、语言 cookie、邀请有效/过期/重复/无效与 SMTP 投递 |
| Candidate Dashboard / Material / Knowledge / Privacy | 12 | GET/POST/PUT/PATCH/DELETE 的身份、输入、成功、失败、幂等、retry、即时撤权与内容访问 |
| Candidate Agent / Publication | 9 | preview/settings/chat/feedback、publish/current/revoke、普通 RAG 与 async deep、SSE 终态/重连/失效 |
| Repository / Dossier / Source | 8 | public/private sync、完整 SHA、visibility/公开深度开关、Dossier projection/approve/rerun、不可变源码预览 |
| Public Agent | 8 | publication/session/chat/thread/feedback/suggestion、material/source Citation、SSE、配额与 publication 撤销 |
| Platform Admin | 15 | overview、Candidate/Agent/Repository/run 治理、reports/reviews/search/settings/invitation 与私有内容隔离 |

分组可重叠于跨边界 route；总路径与 method 必须以 verifier 输出的 `64 / 70` 为准，不能把分组求和当成精确总数。

## Package 入口矩阵

以下 27 个当前入口必须逐项执行或明确标记 `BLOCKED`，不能用一次 `npm test` 代替：

- Fixture：`e2e:fixture`、`e2e:admin-fixture`、`e2e:public-thread-fixture`；
- 基础与文档链路：`smoke:api-surface`、`smoke:auth`、`smoke:connect`、`smoke:job-lease`、`smoke:material-lifecycle`、`smoke:upload`、`smoke:runtime-state`；
- Candidate/Public/Admin：`smoke:agent-preview`、`smoke:agent-runtime-acceptance`、`smoke:privacy-api`、`smoke:publication`、`smoke:public-chat`、`smoke:admin`；
- Repository/Dossier：`smoke:repository-api`、`smoke:repository-dossier`、`smoke:repository-dossier-api`、`smoke:repository-retention`；
- Code Agent：`smoke:analysis-scheduler`、`smoke:analysis-sse`、`smoke:analysis-governance`、`smoke:code-agent-sandbox`、`smoke:repository-analysis-runner`、`smoke:fixed-repository-wiki`；
- 生命周期：`verify:docker-lifecycle`。

## 前置状态

1. 当前分支与工作树已对账，production build、全量单测和 surface verifier 通过。
2. 对现有 PostgreSQL 做删除影响审计与可恢复备份；只有用户批准后才移除 `SPEC-002` 明确退役的旧 GitHub material 数据并应用 0012–0017 migration。
3. 使用 `scripts/docker-up.sh -d` 保留 `askme_local_pgdata`、`askme_local_uploads` 与现有用户数据；记录部署前后材料、会话、消息、发布与账号计数。
4. 使用 `scripts/agent-runner.sh` 在宿主启动 runner；Web/worker/runner 共享同一 PostgreSQL 与 Repository Artifact host root，BoxLite 读取已锁定 image digest。
5. `~/.env` 只在宿主保存 `ASKME_AI_API_KEY` 与 `ASKME_GITHUB_TEST_TOKEN`；Scenario 和日志只记录键是否存在，不读取、输出或持久化值。
6. public fixture 固定 `QuantumNous/new-api@ccd535ef8e50cf6e5846a59278c40b7ff59d1b7d`；private fixture 固定 `monshunter/copybook@10abc90f0d244485c0983a79f0c79238671bd3f0`，固定 SHA 不可获取时直接失败。

## 执行顺序与预期

1. 运行 `verify:surface-matrix`、lint、typecheck、全量 unit test 和 production build。
   - surface 数量精确一致；构建零 error/warning；未运行项不写 PASS。
2. 运行所有 scratch PostgreSQL、AI SDK、BoxLite 与 runner smoke。
   - migration 从空库应用；SSE snapshot/version/重连/invalidated 成立；过期 lease 能重领；stale microVM 清理后才进入 terminal；每 run microVM 不复用。
3. 获批后执行数据迁移和保留 volume 部署，启动 host runner。
   - `db/web/worker` 与 runner 心跳正常；主 readiness 和 Code Agent capability 可区分；部署前后非目标数据计数一致。
4. 逐项运行 27 个 package 入口，并按 `scripts/verify-surface-matrix.ts` 的 route evidence owner 对账 70 个 method。
   - 每个 API 验证匿名、错误角色、有效角色、有效输入、代表性无效输入、稳定错误码、幂等/重试和当前权限撤销；不把一个 happy path 扩大为整个 route PASS。
5. 同步固定 public Revision，生成并批准 Dossier，执行约 10 个固定问题。
   - 覆盖 RAG/Dossier、deep、Router 歧义、证据不足、拒绝、Citation 与历史权限投影；答案事实和最低 Citation 条件成立，不要求逐字一致。
6. 使用一次性 private Token 同步固定 private Revision，完成私有可见性提升、deep、源码 Citation、降权和撤销。
   - Token 不进入数据库、artifact、日志、错误、Web/worker/runner 常驻环境或 guest 持久状态；完整 SHA 固定，撤权即时，artifact 只读且 retention/GC 精确。
7. 在真实浏览器依次执行 anonymous、Candidate 与 Admin 页面矩阵，桌面使用 `1440 × 1000`，移动使用 `430 × 932`。
   - 每页检查主用户功能、导航、loading/empty/error/disabled/terminal 状态、对话框键盘行为、console error/warning、失败 network、可访问名称、focus、横向 overflow 与 responsive geometry。
8. 执行 Candidate → publication → Visitor → Admin 跨角色旅程和 [SCN-001](SCN-001.md) 全量回归。
   - 状态在 UI、API 与数据库一致；publication/visibility/account 撤销立即作用于新请求、SSE 和历史 Citation；Admin 不接收私有正文。
9. 对账 `SPEC-002` 当前验收 Checklist、全部 surface、package 入口、Plan、Review、Progress、Git 与清理状态。
   - 只有所有必需 Evidence 当前且一致时把本场景改为 `verified`；任一缺失保持 `ready_for_execution` 或标记精确 `BLOCKED`。

## 观测与失败判定

- 保存 revision、环境、URL、viewport、关键截图、console/network 摘要、API/数据库状态和清理边界；截图不单独证明后端链路。
- 任何漏页、漏 method、漏 package 入口、使用 mock 代替要求的真实组件、固定 SHA 漂移、Secret 出现在输出、源码进入 Chunk/RAG、microVM 跨 run 复用、失败后无提示 RAG fallback、撤权后仍可访问、Admin 暴露私有内容、移动端横向溢出或未运行却报告 PASS 均为 `FAIL`。
- 外部 AI、GitHub credential 或破坏性 migration 未获授权时为 `BLOCKED`，不得替换为更容易的假验收。

## 清理与恢复

- 删除本场景创建的 fixture Candidate、Repository、publication、conversation、message 与临时 Artifact；只删除能由本场景 id 精确定位的资源。
- 停止隔离回归临时启动的 runner 并确认没有残留 microVM；最终保留数据部署的 host runner 与 `askme-local` Compose 服务继续运行，既有 volumes 保留。
- 不 reset PostgreSQL、不 prune Docker、不删除既有上传；若获批的数据迁移或部署结果不可接受，先停止应用写入，再从 Human Gate 中校验过的全库备份恢复。

## 2026-08-13 部署前 Evidence

- `npm run verify:surface-matrix`：PASS，精确识别 18 个页面、59 个 API 路径 / 65 个 method、25 个 package 验证入口、3 个浏览器角色和 2 类 viewport。
- `npm test`：62 个测试文件 / 210 个测试 PASS；`npm run lint`、`npm run typecheck` 与 `npm run build` PASS，production build 24 个页面、零 warning。
- `smoke:analysis-sse`：scratch PostgreSQL 应用 16 个 migration 后，initial snapshot、NOTIFY wakeup、version 单调、terminal close、reconnect snapshot、authorization invalidation 和 safe payload PASS。
- `smoke:analysis-governance`：scratch PostgreSQL 上 Admin safe projection、disable cancellation、幂等 disable/cancel、runner/Artifact/BoxLite health 与 usage PASS。
- `smoke:analysis-scheduler` Red/Green：修复前明确失败于 expired + cancel-requested run 无法重领；修复后 realtime priority、reserved slot、global concurrency、quota、pending cancellation、expired lease reclaim、expired cancellation reclaim，以及 stale microVM cleanup-before-cancelled PASS。
- `smoke:code-agent-sandbox`：真实 BoxLite microVM 与 Pi tool loop PASS，只加载 `find/grep/ls/read`，Repository 指令不生效，无效 Citation 只修正一次，microVM 已删除，credential 未持久化。
- `smoke:repository-analysis-runner`：真实连续 microVM 中 Dossier 与 conversation deep analysis PASS，Host Citation、cleanup-before-commit、queue 幂等、消息持久化、深度结论不写 Dossier 与 immutable source preview 均成立。
- `smoke:repository-retention` 与 `smoke:repository-dossier` 已在下述隔离全栈环境执行并 PASS；当前保留数据部署仍受 Human Gate 约束。

## 2026-08-13 隔离全栈回归 Evidence

- 使用独立本地数据库 `askme_spec002_e2e_20260813` 从空库应用全部 16 个 migration；Web 运行于独立 `3200` 端口，真实 worker、host-native Agent Runner、Artifact Store 与锁定 digest `sha256:690cf7b245a2418fa24d9be3902c666cd233f03280d2564750fcaab75e4e75c9` 的 BoxLite image 同时运行。`/api/health/ready` 返回 database、migration、worker、runner、artifact、boxlite、ai 全部 `ready`，`codeAgent=ready`。
- 应用问答与 ingestion 使用本地确定性 OpenAI-compatible fixture，只验证 SDK 协议、Router/RAG/organization 状态机、持久化与 Citation，不作为 DeepSeek 外部回答质量 Evidence。真实 BoxLite/Pi microVM、Dossier 与 conversation deep run 仍由独立 `smoke:code-agent-sandbox` 和 `smoke:repository-analysis-runner` 验证。
- 25 个 package 入口中 24 个已执行：3 个 fixture、全部 21 个 smoke/E2E 入口均 PASS；仅 `verify:docker-lifecycle` 因当前数据库的破坏性旧 GitHub material 删除尚未获批而保持 `BLOCKED`。`smoke:runtime-state` 在 Web/worker 实际重启前后验证 session、上传文件 checksum、job、knowledge、migration 与 request audit 均保持一致。
- `smoke:api-surface` 对 59 个 Route Handler 路径执行 65 个已声明 method 与 59 个未声明 method，共 124 次匿名真实 HTTP 探测；领域 smokes 继续覆盖有效角色、成功/失败、owner 隔离、幂等/重试、权限撤销和安全错误码。回归发现登录 JSON 输入曾把 ZodError 投影成 500，已用 Red/Green 测试修复为 `INVALID_JSON` / `INVALID_CREDENTIALS_INPUT` 400。
- `smoke:agent-preview` 的历史 `Agent Preview` 文案断言与 `SPEC-001` 当前 `Agent / 智能体` 单一入口漂移，已改为稳定的 `Candidate Agent` 页面能力断言；`smoke:public-chat` 曾错误要求公开响应泄漏内部 `chunkId`，已改为验证来源标题存在且内部 id 不暴露。两项复验 PASS。
- `smoke:repository-retention` 首次暴露 fixture 缺少必填 `configured_model` 且失败后遗留无 owner artifact；补齐当前 schema 快照并在 `finally` 精确清理 5 个确定性 artifact key 后 PASS，验证 active、Dossier、run、message Citation 四类引用保护以及 5 个 Revision 精确 release/collect。
- Codex in-app Browser 在 `1440 × 1000` 与 `430 × 932` 下逐项打开 18 个页面，共 36 个角色/viewport 页面状态；anonymous、Candidate、Admin 全部无横向 overflow，整个受控 Tab 新增 console error/warning 为 0。无效 invitation、未登录重定向、空 Repository、已填充 Material/Knowledge、公开 Agent 与 Admin 安全投影均可见。
- 浏览器完成 Candidate 隐私首次确认、visibility 变化使确认失效、重新确认、直接发布、公共 Agent 问答、公开 Citation 去内部标识、Markdown 安全渲染、PDF `0.707077` A4 比例、分享 URL 精确写入 clipboard、撤销后旧 slug 立即不可用；Admin 完成 Candidate 搜索、私有正文禁区、Repository/run 安全投影与 open → reviewing → resolved 审核状态机。
- 最终门禁再次 PASS：surface `18 / 59 / 65 / 25`，Vitest `62 files / 210 tests`，lint、typecheck、production build（24 个页面、零 warning）与 `git diff --check`。隔离 browser/admin fixture 已精确清理，scratch database 已 drop，临时目录已移动到 `/Users/tanzhangyu/.Trash/askme-spec002-e2e.VgENh6-20260813` 可恢复；现有 `askme-local` db/web/worker 与 `askme` 数据库未修改并保持运行。

## 2026-08-14 最终验收 Evidence

- 用户已批准删除旧 GitHub material 并完成保留数据迁移；0012–0017 在主数据库与空库均应用，`verify:docker-lifecycle` PASS。最终 `scripts/docker-up.sh -d` 重建 Web/worker/migrate 但保留 PostgreSQL、uploads 与 Repository Artifact；部署前后 users `2`、materials `2`、repositories `2`、revisions `2`、dossiers `4`、wiki pages `20`、projections `2`、conversations `3`、messages `26`、publications `6`、migrations `17` 逐项一致。
- 宿主 Runner 使用锁定 image digest `sha256:a192b1c6bf9ec39bcacaf8ecdda4d94c15313a9896d93f60a4c3261027cb5595` 运行。最终 readiness 的 database、migration、worker、runner、artifact、boxlite、provenance 和 AI 均为 `ready`，Code Agent capability 为 `ready`；默认运行参数为 50 rounds、80 tools、1,000,000 context、200,000 output、Repository 日配额 10。
- 固定 public `QuantumNous/new-api@ccd535ef8e50cf6e5846a59278c40b7ff59d1b7d` 的 version 27 生成 7 个 Wiki 页面、35 个实质 H2、30 个跨子系统 examined paths、12 个 covered areas、77 个源码 Citation、Mermaid 和 35,158 bytes Markdown；10 个基准问题的 route、事实和最低 Citation 全部 PASS。
- 固定 private `monshunter/copybook@10abc90f0d244485c0983a79f0c79238671bd3f0` 的 version 4 生成 6 个 Wiki 页面、33 个实质章节、34 个 examined paths、8 个 covered areas、21 个源码 Citation和 10,034 bytes Markdown；一次性 Token 只用于同步，固定 SHA、撤权、清理与数据库/artifact/log/持久目录泄露扫描全部 PASS。
- surface verifier 当前精确识别 18 个页面、59 个 API 路径、65 个 method、26 个 package 验证入口、3 个角色和 2 个 viewport。3 个 fixture、22 个 smoke/E2E 与 1 个 Docker lifecycle 入口均已当前执行并 PASS；最后补齐的 public-thread fixture 通过真实登录、隐私确认、发布、匿名访客 session 和线程 seed 链路。
- 最终门禁 PASS：Vitest `68 files / 241 tests`、ESLint、Next typegen + TypeScript、`git diff --check`、production build `24 / 24`；Compose 网络内 auth 验证 live/ready 200、Candidate/Admin session、角色 guard、logout 撤权，真实外部 AI check 使用 `deepseek-v4-flash` PASS。
- 最终部署浏览器复验 Candidate Repository UI：页面只有目录标题处一个“添加仓库”按钮，新增表单位于 modal，Repository 以卡片展示，无刷新按钮；desktop/mobile 均无横向 overflow。Repository Citation 点击前后标签页数量保持 1，当前页 Markdown 弹窗显示 Repository、完整 SHA、path/range 与真实 Go 源码，不显示 raw JSON；Escape 后焦点返回原 Citation，console warning/error 为 0。
- 固定输入、真实 BoxLite、Pi Wiki copy-out、Host validator、Approved Wiki 统一知识库、Public/Candidate 深度问答、SSE、配额、历史 Citation 重投影、Admin 安全治理、全部 18 条 AC 和清理边界已由 [REVIEW-075](../reviews/REVIEW-075.md) 复核，结论 `PASS`。本场景没有剩余 Blocker。

## 2026-08-14 OBJ-012 增量验收 Evidence

- surface verifier 当前精确识别 22 个页面、64 个 API 路径、70 个 method、27 个 package 验证入口、3 个角色和 2 类 viewport；Vitest `84 files / 286 tests`、lint、typecheck、production build 与 `git diff --check` PASS。
- Compose 网络内 `smoke:auth` 完成 Candidate 注册、登录、注销、忘记密码、一次性重置、旧 session 撤销、登录后改密与替代 session；同一次验收通过 Mailpit 分别观察到密码重置和 Admin invitation 邮件，验证两类模板复用统一 SMTP transport。
- `smoke:public-chat` 验证同一 visitor token 恢复 Conversation、缺少 localStorage owner 时不从全局 cookie 恢复、两个游客消息与反馈隔离、伪造来源拒绝、过期 Conversation 新建但不旋转浏览器 token，以及 publication/来源撤权即时生效。
- `smoke:agent-runtime-acceptance` 对固定问题“copybook 的 paginate 函数在分页时如何处理剩余格子？”完成 Candidate/Public 两条真实 Conversation Deep；Candidate 引用 `src/lib/pagination.ts` 和测试文件，Public 安全投影显示对应来源标题，route audit 为 `deep/source_inspection_required`，问答不消耗日次数。
- 一次 Public run 的首个结果触发 `CODE_ANSWER_CITATION_HASH_INVALID`，Runner 使用剩余预算重新读取精确范围后 correction 成功；另一次 Runner 退出留下的过期 lease 和 microVM 由新 Runner 先清理再重领完成。最终相关 run 均 `completed/cleaned`，`data/boxlite/boxes` 为 0，readiness 全部 `ready/configured`。
- Codex in-app Browser 在 430 × 932 完成注册、改密、注销、忘记密码、Mailpit reset link、重置后登录，以及公开 Agent 同浏览器跨刷新/标签会话恢复。最终页面可见回答明确说明剩余格子不会丢弃或补齐，而是形成较短的最后一页，并显示 `src/lib/pagination.ts:22-24` 与两段测试 Citation；无横向 overflow，console warning/error 为 0。
