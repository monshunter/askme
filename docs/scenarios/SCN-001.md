# SCN-001：Candidate Agent 单一入口与发布闭环

状态：`verified`

关联 Plan：[PLAN-009](../plans/PLAN-009.md)、[PLAN-011](../plans/PLAN-011.md)

关联验收：[SPEC-001 `AC-PRIV-002`、`AC-AGENT-004`、`AC-PUB-001`、`AC-PUB-003`、`AC-UI-004`、`AC-UI-005`、`AC-UI-006`](../specs/SPEC-001.md)

## 用户结果

Candidate 只通过 Agent / 智能体一级入口完成预览、设置、直接发布、访问和撤销；Workspace 不再显示快捷操作、邀请面试、独立发布入口或 Candidate Agent 链接模块，旧发布页面和链接生成 API 不可访问。公开 Agent 页面提供复制当前 URL 的分享操作，不再下载链接文件。语言切换在登录前后都只由右上角全局控件提供，页面、footer 与账号菜单没有第二入口；英文品牌保持 Askme，中文品牌统一为“职问”。

## 交互上下文

- 角色：本地 Candidate fixture
- 环境：已通过生产构建的本地 Web、健康 PostgreSQL 与 worker；入口由 `ASKME_BASE_URL` 指定
- 浏览器：Codex in-app Browser；本地隔离页面不依赖用户个人浏览器会话
- Viewport：桌面 `1448 × 1086`，移动 `430 × 932`
- Fixture：`npm run e2e:fixture -- prepare`；登录信息由项目 fixture owner 提供，不写入 Scenario Evidence
- 自动化入口：`npm run smoke:publication` 覆盖同一 publication API、登录/Agent/公共页 SSR 的全局唯一语言入口与旧路由 404；`global-language-switcher-contract.test.ts` 证明只有 Root Layout 渲染组件

## 前置状态

1. 当前 revision 已通过 `next build` 并由本地 Web 进程运行，db/web ready、worker running。
2. Candidate fixture 已重建，包含一份 `public_preview` 的 indexed 资料和可用公开身份，尚未确认当前隐私策略、未发布 Agent。
3. 浏览器从干净的 fixture 登录开始，不复用其他 Candidate 状态。

## 步骤与预期

1. 未登录访问 `/login`，在右上角将 English 切换为简体中文，再切回 English。
   - 页面可见且只可见一个语言选择控件，位置位于 viewport 右上角；切换刷新后登录页文案与 `<html lang>` 一致。
   - 登录卡片内部没有第二入口，切换不要求登录且不改变当前 URL。
   - wordmark 显示 Askme / 职问，页面不显示旧中文品牌“问候”。
2. 登录 Candidate 后打开 `/workspace/agent`。
   - 一级导航只有 Dashboard、Upload Materials、Knowledge Base、Privacy Control、Agent；页面 `h1` 为 `Agent`。
   - 顶栏不存在 Quick Action，侧栏不存在 Invite Interviewers 与 Publish Agent。
   - 账号菜单只有 Candidate 身份信息与 Sign out；页面 footer 没有语言入口，右上角全局控件仍是页面唯一实例。
   - Candidate wordmark 与 hero 印章显示“职问”，不存在旧中文品牌。
3. 检查 Agent 页面问答、settings 和发布区域。
   - 页面同时显示预览对话、Answer Tone/Public Mode/Privacy-Safe Mode 与当前数据库计算的发布 readiness。
   - 未确认隐私时发布动作被阻断并指向 Privacy Control。
4. 在 Privacy Control 确认当前策略、修改一次来源可见性并再次确认后，返回 Agent 页面直接发布。
   - 当前修订确认后不显示“Confirm Again / 再次确认”；来源可见性变化使 revision 失效后才显示该操作，再次确认后 readiness 变为 Ready to publish。
   - 页面不显示 Candidate Agent Link / 候选人 Agent 链接模块；Publish Agent 直接生成 opaque slug 并发布。
   - 发布成功后同页显示 Published、Visit Agent / 访问Agent 与 Revoke Access / 撤销访问，访问操作位于撤销操作左侧，Public Mode 同步显示 On。
5. 通过 Visit Agent / 访问Agent 打开公开页，复制分享 URL 后返回 Agent 页面并撤销访问。
   - 公开页使用 Interviewer 权限且能显示 fixture 的公开身份；右上角仍只有同一个全局语言控件，public footer 没有第二入口，wordmark 与 hero 印章显示“职问”。
   - 左下角显示 Share Agent Link / 分享Agent 链接；点击后 clipboard 精确等于当前公开页 URL，页面反馈复制成功且不触发文件下载。
   - 撤销确认可由键盘/按钮完成；撤销后旧公开链接不可用。
6. 使用右上角全局控件切换为简体中文，并以 `430 × 932` 检查 Agent 页面。
   - 一级导航和 `h1` 使用“智能体”；Shell 仍没有“快捷操作”“邀请面试官”“发布 Agent”一级入口。
   - 全局控件不与 drawer、搜索、头像或页面操作重叠；`documentElement.scrollWidth` 不超过 `innerWidth`，主要问答、设置与发布区域可纵向访问。
7. 直接访问 `/workspace/publish`、`/workspace/publish/preview`、`GET /api/publications/preview` 与 `POST /api/publications/link`。
   - 两个旧页面、专用 Candidate 公共预览 API 和链接生成 API 均返回 404，不渲染旧发布 UI，也不保留无消费者的旧后端。

## 观测与失败判定

- 记录页面可见文本、导航链接、语言控件数量与 bounding rect、viewport、顶栏交叠和横向 overflow。
- 检查运行期间新增的 console error/warning 与失败网络请求；预期旧路由 404 除外。
- 任一页面出现第二语言入口、全局控件不在右上角或与顶栏重叠、旧中文品牌“问候”仍作为品牌标记可见、重复 Candidate 入口或 Candidate Agent 链接模块仍可见、链接生成 API 非 404、确认按钮状态错误、访问与撤销顺序错误、分享未复制当前 URL、发布/撤销/Public Mode 状态不一致或移动端横向溢出均为 `FAIL`。

## 清理与恢复

- 执行 `npm run e2e:fixture -- cleanup` 删除本场景 Candidate 及级联 publication、conversation、material 与 knowledge 状态。
- 保留 `askme-local` 容器与既有 volumes，不 reset、不删除其他本地用户数据。

## PLAN-009 Evidence

- Revision：`HEAD 7ce39ec + PLAN-009 working tree`；日期：2026-08-12。
- 环境：宿主机 `next build` 通过后以 `next start -p 3100` 运行新 revision；`/api/health/live` 与 `/api/health/ready` 均成功，ready 明确报告 database、migration、worker、AI 为 `ready/configured`。Docker BuildKit 曾在同一 `next build` 阶段无输出停留 6 分钟，已停止该构建；宿主机同 revision 生产构建 6.3 秒通过，因此未把 BuildKit 卡顿记录为代码 PASS 或产品失败。
- 自动化：完整 Vitest `43 files / 143 tests`、`npm run typecheck`、`npm run lint`、`npm run build` 与 `git diff --check` 通过；`smoke:publication` 报告 `singleGlobalLanguageSwitcher=true`、`agentPublicationRendered=true`、`retiredPublishPagesUnavailable=true`、`revoked=true`。
- 未登录桌面 `1448 × 1086`：`/login` 的全局语言控件 bounding rect 为 `top=24,right=24`，页面仅 1 个 switcher；English → 简体中文 → English 均保持 `/login`，`html lang` 分别为 `zh-CN/en`，中文首屏显示 Askme / 职问且无旧品牌“问候”。
- Candidate 桌面 `1448 × 1086`：可见一级导航仅 Dashboard、Upload Materials、Knowledge Base、Privacy Control、Agent；无 Quick Action、Invite Interviewers、Publish Agent，账号菜单无语言控件，全页 switcher 数为 1；全局控件与 profile rect 不相交，`scrollWidth=clientWidth=1448`。
- Agent 发布闭环：未确认隐私时 readiness 指向 Privacy Control；确认后同页生成 opaque link 并发布，Published、Open Public Page、Revoke Access 可见，且 Public Mode 同步为 `on`；公共页只显示 fixture 公开身份，switcher 数为 1，Askme / 职问 wordmark 与“职问”hero 印章可见；撤回后 Public Mode 为 `off`，旧链接显示不可用。
- 中文移动 `430 × 932`：`html lang=zh-CN`，一级导航与 `h1` 为“智能体”；全局控件 `top=13,right=12,width=113,height=42`，不与 drawer summary 或 profile 相交；无快捷操作、邀请面试官、发布 Agent 一级入口，所有品牌标记为“职问”，`scrollWidth=clientWidth=430`。
- 退役路由：浏览器直接访问 `/workspace/publish` 与 `/workspace/publish/preview` 均显示 404，且仍由根布局提供唯一全局语言控件；publication smoke 同时证明 `GET /api/publications/preview` 返回 404。
- Browser console：本场景新增 error/warning 为 0。
- 清理：`npm run e2e:fixture -- cleanup` 完成，临时 `next start -p 3100` 进程已停止；保留既有 `askme-local` 容器与 volumes。
- 截图：[Candidate Agent 桌面发布态](evidence/plan-009/candidate-agent-desktop.png)；[Candidate Agent 中文移动态](evidence/plan-009/candidate-agent-mobile-zh.png)。

## PLAN-011 增量 Evidence

- Revision：`HEAD 2800607 + PLAN-011 working tree`；日期：2026-08-12。
- 环境：当前 production build 在隔离 `3100` 端口运行，`live=200`，ready 明确报告 database、migration、worker 为 `ready`、AI 为 `configured`；测试结束后临时进程已停止，既有 `3000` Docker 服务未重启或覆盖。
- 自动化：完整 Vitest `44 files / 148 tests`、`npm run typecheck`、`npm run lint`、`npm run build` 与 `git diff --check` 通过；privacy smoke 报告 `reconfirmationVisibleOnlyAfterChange=true`；publication smoke 报告 `retiredLinkApiUnavailable=true`、`directPublish=true`、`legacyDraftCompatible=true`、`republishedWithNewSlug=true`。
- 隐私确认：首次确认后“再次确认”按钮数量为 0；来源从 `public_preview` 改为 `citation_allowed` 后“再次确认”可见，再次确认后发布 readiness 恢复。
- Candidate 发布：页面不存在“候选人 Agent 链接”标题；直接发布成功后“访问Agent”矩形为 `left=944.31,right=1083.99`，“撤销访问”矩形为 `left=1091.99,right=1213.99`，同一行且访问操作在左。
- 公开分享：公开 URL 与“访问Agent”href 一致；点击“分享Agent 链接”后页面反馈“Agent 链接已复制。”，隔离 clipboard 哨兵被精确覆盖为当前公开 URL。
- 移动公开页 `430 × 932`：`scrollWidth=clientWidth=430`，“分享Agent 链接”控件 `left=12,right=418,width=406`，无横向溢出。
- 撤销：确认对话可见且完成后旧公开 URL 显示“此 Agent 暂不可用”；Candidate 与 Public 页 console error/warning 均为 0。
- 清理：隔离浏览器 tabs、fixture Candidate 及临时 `3100` 进程均已清理；保留 `askme-local` 容器与 volumes。
